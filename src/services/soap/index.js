// import { createClientAsync } from 'soap';
// import fetchVooWebService from './voo';

/**
 * Serviço desabilitado, motivo: aguardando resposta da infraero
 */
export default async function infraeroSoap() {
  // const url =
  //   'http://voos.infraero.gov.br/wsvoosmobile/ConsultaVoos.svc?singleWsdl';
  // try {
  //   const client = await createClientAsync(url);
  //   return {
  //     fetchVooWebService: fetchVooWebService(client),
  //   };
  // } catch (error) {
  //   throw new Error(error);
  // }
}
